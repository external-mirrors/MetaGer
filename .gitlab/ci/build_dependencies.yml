build_dependencies_npm:
  stage: build_dependencies
  image: node:14
  script:
    - cd metager
    - npm install --cache .npm --prefer-offline --no-audit
    - npm run prod
  artifacts:
    paths:
      - metager/public
    expire_in: 2 hrs
  cache:
    paths:
      - metager/.npm/


  