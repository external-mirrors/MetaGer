build_dependencies_npm:
  stage: build_dependencies
  image: node:14
  script:
    - cd metager
    - npm install
    - npm run prod
    - ls -alh /root/.npm
  artifacts:
    paths:
      - metager/public
    expire_in: 2 hrs
  cache:
    paths:
      - /root/.npm


  